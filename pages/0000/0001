///////////////////////////////////////////////////////////////////////////////
//  frikt/\t^/p465s/0000/0001
//
//  TT T T hiss was the-_-- [[firsS-t.
//

    @include "default_layout";
    
    0 goto->title_screen;


    setfg(255, 255, 255, 255);

    horizontal_string = global->title_screen->string;
    horizontal_bottom = global->title_screen->bottom;

    page_delay = 1;
    return_val = putstring(1, 1, here->horizontal_string, here->page_delay, true);

    if (here->return_val != "")
        here->page_delay = 0;

    putstring(here->horizontal_bottom, 1, here->horizontal_string, here->page_delay, true);

    setbg(32, 127, 220, 255);

    putcolumn(
        3,
        1,
        32,
        (global->title_screen->display_height - 5),
        " "
    );

    putcolumn(
        4,
        2,
        30,
        (global->title_screen->display_height - 6),
        load("pages/0000/0001")
    );
