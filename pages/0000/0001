///////////////////////////////////////////////////////////////////////////////
//  frikt/\t^/p465s/0000/0001
//
//  TT T T hiss was the-_-- [[firsS-t.
//

    num = 0;
    row = 10;
    column = 10;

    string = "Hello, world!";

    block print_string;
    //  Grab the next byte in the string.
    //
        byte = substring(here<-string, here<-num, (here<-num + 1));

    //  Increment the column position.
    //
        here<-column = (here<-column + 1);
        here<-num = (here<-num + 1);

    //  Print the byte.
    //
        putchar(here<-row, here<-column, here->byte);
    endblock;

    90 print(strlen(here->string));
    91 print(here->num);
    
    100 if (here->num >= strlen(here->string))
        goto->200
    else
        goto->print_string;

    goto->90;

    200 putstring(5, 5, "Done!");
