///////////////////////////////////////////////////////////////////////////////
//  frikt/\t^/p465s/0000/0001
//
//  TT T T hiss was the-_-- [[firsS-t.
//

    string = str = "A string";
    var = 20;

    block [x] test;
        string = "A different string";
        var = (self<-var * 10);

         block [x] inner;
            string = "Yet another string";
            var = (self<-var * 10);

            print(`The root string is $parent<-string`);
            print(`The parent string is $parent->string`);
            print(`The parent var is $global->var`);
        endblock;
    endblock;

    test [x];

    print(here->string);
    print(here->str);
    print(here->var);

    print(here->test->string);
    print(here->test->var);

    print(here->test->inner->string);    //  These lines won't
    print(here->test->inner->var);       //  work (private).

