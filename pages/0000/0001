///////////////////////////////////////////////////////////////////////////////
//  frikt/\t^/p465s/0000/0001
//
//  TT T T hiss was the-_-- [[firsS-t.
//

    1 result = load("test_file");
    2 if (here->result == 1) print("Error loading file")
    else print(here->result);

    num = 0;
    row = 10;
    column = 10;

    string = "Hello, world!";
    str = "";

    block print_string;
    //  Grab the next byte in the string.
    //
        byte = substring(here<-string, here<-num, (here<-num + 1));

    //  Increment the column position.
    //
        here<-column = (here<-column + 1);
        here<-num = (here<-num + 1);

    //  Print the byte.
    //
        putchar(here<-row, here<-column, here->byte);

        global->str = global->str . here->byte;

    endblock;

    100 if (here->num >= strlen(here->string))
        goto->200
    else
        goto->print_string;

    goto->100;

    200 putstring(5, 5, "Done!");
    putstring(6, 5, global->str);
