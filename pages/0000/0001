///////////////////////////////////////////////////////////////////////////////
//  frikt/\t^/p465s/0000/0001
//
//  TT T T hiss was the-_-- [[firsS-t.
//

    num = 0;
    string = "abcdefgh";

    print(here->num);

    block [x] print_string;
        here<-num = (here<-num + 1);
        putchar(10, (10 + here<-num), substring(here<-string, 0, 1));
    endblock;

    // if (here->num < 8) goto->print_string;

    // print(here->num);
