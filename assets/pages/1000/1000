`
   setpair link
            16 128 220 255
            0 0 0 255;
   setpair page
            255 255 255 255
            0 0 0 255;
   setpair bullet
            220 220 255 255
            0 0 0 255;
`Visit my `bold; enablepair link; site https://github.com/Friktata`Github`site; disablepair; bold;` profile.

I wanted to learn `bold`SDL3`bold` but I also wanted to learn `bold`Lua`bold`, so I build `bold`SDLua`bold`.

It's exactly what it sounds like - SDL bindings for the Lua scripting language, some features:

   - `enablepair bullet`Create surfaces and textures.`disablepair bullet`
   - `enablepair bullet`Load/render images of various types.`disablepair bullet`
   - `enablepair bullet`Load and play audio (.mp3, .wav, etc).`disablepair bullet`
   - `enablepair bullet`Event handling for mouse, keyboard, etc.`disablepair bullet`
   - `enablepair bullet`Font loading and rendering.`disablepair bullet`

There are a lot more features, SDLua is great for knocking up prototypes of building simple desktop applications, you can even build simple games with it.

If you want to know more visit the repo at:

   `bold; enablepair link; site https://github.com/Friktata/sdlua`github.com/Friktata/sdlua`bold; disablepair link; site`

You'll also find a detailed tutorial on page `bold; underline; enablepair page`1100`bold; underline; disablepair page`.

 
  